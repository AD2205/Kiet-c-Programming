#include<stdio.h>
void swap(int *p,int *q)
{
    int temp;
    temp=*p;
    *p=*q;
    *q=temp;
    printf("%d  %d  \n",*p,*q);
}
int main()
{
int a=20,b=30;
swap(&a,&b);
printf("%d  %d  \n",a,b);

}
